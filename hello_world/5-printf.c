/**
 *main - prints sentance
 *
 *Return: Always 0
 */

#include <stdio.h>
#include <string.h>

int main(void)
{
char str[] = "with proper grammar, but the outcome is a piece of art,";
printf("%s\n", str);
return (0);
}
