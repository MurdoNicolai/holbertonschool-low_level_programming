#include <string.h>
#include <stdio.h>

/**
 *puts2 - prints every other caracter
 *
 *@str: string to print
 *Return: void
 */

void puts2(char *str)
{
	int c;

	for (c = 0; c < strlen(s) ; c += 2)
		printf("%c", s[c]);
	printf("\n");
}
