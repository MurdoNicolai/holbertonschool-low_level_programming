/**
 *main - prints sentance
 *
 *Return: Always 0
 */

#include <stdio.h>
#include <string.h>

int main(void)
{
char str[] = "\"Programming is like building a multilingual puzzle\n";
puts(str);
return (0);
}
